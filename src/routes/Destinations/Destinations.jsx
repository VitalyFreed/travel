import React from 'react';

import styles from './destinations.module.scss';

const Destinations = () => {
    return (
        <div>
            <h1>Destinations</h1>
        </div>
    );
};

export default Destinations;